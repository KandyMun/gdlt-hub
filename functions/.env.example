# Public Discord Client ID (safe to commit for reference; the real one goes in functions/.env)
DISCORD_CLIENT_ID=
# NOTE: the Client Secret is NOT here. Set it as a secret:
#   firebase functions:secrets:set DISCORD_CLIENT_SECRET
